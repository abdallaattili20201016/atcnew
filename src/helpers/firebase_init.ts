// import { FirebaseAuthBackend } from "./firebase_auth";
// import { FirebaseStorageBackend } from "./firebase_storage";
// import { FirebaseFirestoreBackend } from "./firebase_firestore";

// let _fireBaseBackend: any = null;

// const initFirebaseBackend = (config: any) => {
//   if (!_fireBaseBackend) {
//     _fireBaseBackend = {
//       auth: new FirebaseAuthBackend(config),
//       storage: new FirebaseStorageBackend(config),
//       firestore: new FirebaseFirestoreBackend(config),
//     };
//   }
//   return _fireBaseBackend;
// };

// const getFirebaseBackend = () => {
//   return _fireBaseBackend;
// };

// export { initFirebaseBackend, getFirebaseBackend };
